[Unit]
After=network.target

[Service]
User=root
Group=www-data
WorkingDirectory=/var/www/flaga
ExecStart=gunicorn --workers 3 --bind unix:flaga.sock -m 007 app:app

[Install]
WantedBy=multi-user.target
